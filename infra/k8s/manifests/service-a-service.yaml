apiVersion: v1
kind: Service
metadata:
  name: service-a
  namespace: sample-app
  labels:
    app: service-a
spec:
  selector:
    app: service-a
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP